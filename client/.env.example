# API base URL (no trailing slash, must include /api suffix)
VITE_API_URL=https://your-api-domain.com/api

# Base URL for static images (uploads served by the server, NO /api suffix)
VITE_IMAGE_BASE_URL=https://your-api-domain.com
